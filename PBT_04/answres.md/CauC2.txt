<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa lỗi Flexbox Layout</title>

    <style>
        *{
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        body{
            background: #f5f5f5;
            padding: 30px;
        }

        h1{
            margin-bottom: 30px;
            text-align: center;
        }

        section{
            background: white;
            padding: 20px;
            margin-bottom: 40px;
            border-radius: 10px;
        }

        h2{
            margin-bottom: 15px;
            color: #333;
        }

        h3{
            margin: 20px 0 10px;
            color: #444;
        }

        pre{
            background: #eee;
            padding: 15px;
            border-radius: 8px;
            overflow-x: auto;
        }

        /* ===================== */
        /* LỖI 1 */
        /* ===================== */

        .card-container{
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }

        .card{
            width: 30%;
            background: #fafafa;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 15px;

            display: flex;
            flex-direction: column;
        }

        .card img{
            width: 100%;
            border-radius: 5px;
        }

        .card h3{
            margin: 10px 0;
        }

        .btn{
            margin-top: auto;
            padding: 10px;
            background: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        /* ===================== */
        /* LỖI 2 */
        /* ===================== */

        .hero{
            height: 300px;
            display: flex;

            justify-content: center;
            align-items: center;

            background: #333;
            color: white;
            border-radius: 10px;
            margin-top: 20px;
        }

        .hero-content{
            text-align: center;
        }

        /* ===================== */
        /* LỖI 3 */
        /* ===================== */

        .layout{
            display: flex;
            margin-top: 20px;
            border: 1px solid #ccc;
        }

        .sidebar{
            width: 250px;
            flex-shrink: 0;

            background: #ffdddd;
            padding: 20px;
        }

        .content{
            flex: 1;
            padding: 20px;
            background: #ddf0ff;
        }
    </style>
</head>

<body>

    <h1>Sửa lỗi Flexbox Layout</h1>

    <!-- ======================= -->
    <!-- LỖI 1 -->
    <!-- ======================= -->

    <section>
        <h2>Lỗi 1: Cards không đều chiều cao</h2>

        <p>